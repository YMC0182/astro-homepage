---
import { Image } from "astro:assets";
import "../../styles/global.css";

type Props = {
    title?: string;
    description?: string;
};

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <meta name="generator" content={Astro.generator} />
        <title>{ title || "袁某人主页 - MrYuan's Homepage" }</title>
        <link rel="icon" type="image/x-icon" href="/avatar.webp" />
        <meta name="keywords" content="袁某人,袁某,个人网站,个人主页,博客,留言簿" />
        <meta name="description" content={ description || "袁某人的个性空间：嘿！很高兴遇见你。这是我的实时生存报告与人格画像。在这里，文字不只是记录，更是连接。翻翻博客，逛逛留言簿，让我们在这个简约的空间里，开启一场有趣的对话。" } />
    </head>
    <body ontouchstart="ontouchstart" class="font-mono bg-stone-50 text-stone-600 antialiased dark:bg-stone-900 dark:text-stone-400">

        <main class="mx-auto w-full max-w-2xl space-y-9 px-6 py-16 text-base leading-8 break-words">
			<Image src="/avatar.webp" alt="头像" width={100} height={100} class="mb-10 size-6 rounded-full outline-2 outline-offset-2 outline-dashed outline-stone-200 dark:outline-stone-700"/>
            <slot />
		</main>

        {!import.meta.env.DEV && (
            <script defer src="https://stats.betterapi.net/stats.js" data-website-id="d56426d4-f9ff-417a-8745-8eac7006c182"></script>
        )}

    </body>
</html>